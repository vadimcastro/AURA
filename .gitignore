# ── Sui / Move ────────────────────────────────────────────
build/
.sui/
*.mv
*.mv.bcs
Move.lock

# ── Node / TypeScript SDK ─────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.env.*.local

# ── OS ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ── IDE ───────────────────────────────────────────────────
.vscode/
.idea/
*.iml

# ── Hackathon reference (not our content) ─────────────────
sui_hackathon.md
AURA.md

# ── Logs & debug ──────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Keys (NEVER commit) ──────────────────────────────────
*.keystore
*.key
*.pem
sui.keystore

# ── Local planning artifacts (task lists, notes, etc.) ───
artifacts/
deepbook_traces.json
!dashboard/public/deepbook_traces.json
sdk/npm_cache/
dashboard/npm_cache/
sdk/.memwal_cache/

# ── Vite / React / Frontend (Dashboard) ───────────────────
dashboard/dist/
dashboard/dist-ssr/
dashboard/.eslintcache
dashboard/.vite/

# ── Local run state ───────────────────────────────────────
*escalations.json
